// theme.ts
import {  } from "@nextui-org/react";

export const customTheme = createTheme({
    type: "light", // or "dark"
    theme: {
        colors: {
            // Customize the background color for inputs globally
            inputBg: "#333333", // Replace with your desired background color
        },
    },
});
